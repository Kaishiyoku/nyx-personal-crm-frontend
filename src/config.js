/* eslint-disable sort-keys */

const config = {
    backend: {
        baseUrl: 'http://localhost:8000/api',
    },
    fallbackLocale: 'en',
    availableLocales: ['en'],
    appTitle: 'Nyx Personal CRM',
    navTitle: 'Nyx',
};

export default config;
